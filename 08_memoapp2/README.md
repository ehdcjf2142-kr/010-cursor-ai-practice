# 08_memoapp2

디자인을 여러 버전으로 바꿔보기 쉽게 **최대한 단순한 UI**로 만든 메모앱입니다.

## 실행

```bash
npm install
npm start
```

## 특징

- 로컬 저장: `localStorage`
- 기능: 추가 / 수정 / 삭제 / 고정(pin) / 검색
- 스타일: `src/styles.css`에서 CSS 변수만 바꿔도 테마를 쉽게 바꿀 수 있게 구성

